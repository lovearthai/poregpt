# nanopore_llm_workflow/cli/__init__.py
# 可选：暴露所有 CLI 工具的 main 函数（方便测试或内部调用）
# from .fast5.fast5_to_chunks import main as fast5_to_chunks_main
# from .step02_train_vq_model import main as train_vq_main
# __all__ = ["fast5_to_chunks_main", "train_vq_main"]

# 必须存在（即使为空），否则 Python 不认为 cli 是一个包。

